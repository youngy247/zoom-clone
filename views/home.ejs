<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <script>
        const ROOM_ID = "<%= roomId %>";
    </script>
    <script defer src="https://unpkg.com/peerjs@1.2.0/dist/peerjs.min.js"></script>
    <script src="/socket.io/socket.io.js" defer></script>
    <script src="script.js" defer></script>
    <style>
        /* Add your CSS styles here */
        .container {
            text-align: center;
            margin-top: 200px;
        }

        .button {
            padding: 10px 20px;
            font-size: 18px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        .button:disabled {
        opacity: 0.5;
        cursor: default;
        }

        #video-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, 300px);
            grid-auto-rows: 300px;
        }

        video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
    </style>
    
</head>
<body>
    <div id="video-grid"></div>
    <div class="container">
        <h1>Welcome to the Video Chat App</h1>
        <p>Click the button below to start a video call</p>
        <p>Activity Status: <%= isActive ? 'Active' : 'Inactive' %></p>
        <p>Availability Status: <%= isAvailable ? 'Active' : 'Inactive' %></p>
        <% if (isActive && isAvailable) { %>
            <button id="ring-button" class="button">Ring</button>
        <% } else { %>
            <button id="ring-button" class="button" disabled>Ring</button>
        <% } %>
    </div>
    <button id="end-call-button" class="button" onclick="endCall()">End Call</button>
</body>
</html>
